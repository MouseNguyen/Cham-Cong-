Implement the entire approved PAY-W3-01b schedule backend now. You are hermes-forge-w3-01b, sole product writer. Read docs/agent-packets/PAY-W3-01b-HERMES.json first; it is the current exact approved contract and overrides historical deferred-Hermes or intake-only text for this job. Duke approved this next implementation and delegation; do not return another approval request or plan-only result.

Use F:\Codex\Projects\Pay Slip directly; no worktrees or child agents. Existing Forge Terra route only. Read the design, implementation plan, task status, numeric matrix and relevant accepted code. Implement versioned opening hours and employee weekly schedules, per-date/holiday exceptions, and trusted effective queries. Preserve physical history and old snapshots; explicit schedule context is never worked time or payable approval. Use ordinary clear maintainable modules; do not copy minified formatting merely because another module uses it.

Parent is independently implementing the test harness and reviewing DB/schema boundaries while you own only the packet's product/test files. Parent-owned file changes are expected. Start by writing focused failing schedule tests. Existing pure Vitest tests may run using node node_modules/vitest/vitest.mjs run packages/attendance-domain/test/schedule.spec.ts --maxWorkers=1 --no-file-parallelism. For database integration tests, write and reuse synthetic auth/employee fixture patterns (never ambient database URL); parent supplies a fresh project-local harness at scripts/windows/test-schedules.ps1. Update progress as soon as RED tests are ready and when migration/implementation are ready so parent can run them. If a Hermes terminal policy blocks an approved script, report the exact command to parent and continue useful scoped code/tests; do not evade policy or abandon the feature.

At each useful milestone write docs/agent-packets/PAY-W3-01b-HERMES-progress.json, first within a few minutes: current phase, files changed, focused tests, readiness for parent DB execution, exact blockers, next action, session ID. Use your 15-minute checkpoint for a running core, not infrastructure. Limit90 turns/3600seconds; after two same-layer failures or a zero-progress attempt, identify causal evidence and hand the blocked action to parent. No tests claiming success from zero discovered tests.

When implementation and tests are ready, write docs/planning/W3-01b-schedule-contract.md and the structured handoff with precise verification limits. Parent independently verifies, integrates and commits. Do not modify task-status.json, package.json, scripts/windows/test-database.ps1, old migrations, prior receipts, other features or files outside the project. No real records, email, credentials, .env values, new dependencies, servers, network research, browser, recursive deletion, commit, push or global changes.
