Continue PAY-W2-02 now. Duke explicitly approved the consolidated implementation/runtime scope in docs/agent-packets/PAY-W2-02-HERMES-execution-approval.md and instructed us to get it done without more routine questions.

ACTIVE CONTRACT: docs/agent-packets/PAY-W2-02-HERMES-execution.json. It supersedes the older intake-only prompt/packet and pending-approval text. The execution-approval.md file is the reviewed scope, and its approval has now been received. Do not stop at a second plan. Implement and verify the entire Authentication feature, then hand off the real result.

Use the same Forge model/provider/effort and this project-bound task session. One writer, no delegation/fleet or fallback model. Work only in F:\Codex\Projects\Pay Slip and owned paths. Read the active contract and verify source freshness, then resolve official exact dependency versions and record before install, write tests and actual RED, implement, run GREEN and real local Next/Playwright flow, run relevant regressions/typecheck/lint/build, clean owned processes and return evidence. Target the working auth slice first; do not expand infrastructure or polish.

The approved wave includes named dependency metadata/install and Chromium download; official registry/maintainer/CDN access is separate from model transport. Preserve existing direct package versions. Use project-local Chromium cache, only disposable synthetic PostgreSQL127.0.0.1:55432 and Next127.0.0.1:46217, runtime-generated synthetic secrets with CurrentUser DPAPI, no ambient credentials/data. No scheduled identity canary. Tests must be nonempty; no passWithNoTests and no npx implicit install. Protect secure cookies and prove actual routes/browser flow without production bypass. Avoid secret-bearing logs/traces/screenshots. No commit/stage/push or task-status edits.

Write docs/agent-packets/PAY-W2-02-HERMES-progress.json at the first useful milestone and every meaningful milestone/15min checkpoint: current phase, files changed, RED/GREEN counts, blocker, next action, owned process IDs (never secrets). Full run budget120 tool turns/90min; preserve code and safe cleanup on timeout. Two same-layer failures or one zero-delta attempt requires direct root-cause evidence and bounded official research before one failure-removing fallback. Do not blindly retry.

Final artifact docs/agent-packets/PAY-W2-02-HERMES-handoff.json must cover changed paths, direct RED/GREEN/browser/DB/DPAPI evidence, regression/typecheck/lint/build results, cleanup and exact blockers. Do not claim production readiness or completed downstream payroll/attendance workflows. Codex parent will independently inspect and verify your result.
