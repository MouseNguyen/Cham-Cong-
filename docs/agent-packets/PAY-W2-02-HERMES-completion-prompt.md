Resume the approved W2-02 execution. Your prior handoff is rejected as incomplete. Codex has removed the exact lint blocker with a minimal, independently checked change: .tools, .tmp and generated Prisma files are ignored while product files remain linted. Codex also repaired literal redaction placeholders that your earlier rewrite introduced in synthetic DB URLs; now URL.password is assigned in memory. Do not undo these repairs. Input hashes in execution.json are rebound to the current intentional working changes.

There is NO missing approval. Continue the existing full implementation wave; do not ask for another wave or return because the feature is unfinished. An always-401 route and UI skeleton are not the requested result. Do not treat full-repo lint as a prerequisite to actually implementing login. Get the auth DB/API/browser core working, then run the remaining verification. Keep current existing model route, project/file ownership and effect bounds.

Concrete next deliverable: a transaction-backed auth repository/service using the installed pg, argon2, otplib and existing DPAPI adapter; actual login/totp/logout/session handlers; usable owner/accountant login flow. Generate synthetic accounts/credentials at runtime in test support. Use the fresh approved W2-02 DB harness, add necessary tests before implementation, then observe real GREEN and real local Chromium/Next login/TOTP flow. Build one minimal test-auth.ps1 supervisor to keep the same disposable DB available for integration and browser tests; avoid elaborate infrastructure. Production bootstrap may take an explicitly configured connection string at the trusted server boundary; this wave must use only the harness-generated synthetic connection environment, never read ambient credentials.

Acceptance must include failed/locked login and TOTP attempts, password-only challenge denial, atomic challenge/replay consumption under concurrent requests, no session on wrong/replayed OTP, hashed session+CSRF storage, session expiry/revocation/rotation, resource organization checks, owner fresh-MFA and accountant/kiosk denials, invalid/future MFA dates and unknown commands denied, CSRF/origin enforcement on unsafe routes, DPAPI factor ciphertext-only storage, generic/redacted errors. Authorize must never trust a request body actor/role; test actual route/service enforcement. No future payroll implementation beyond the common command authorization boundary. Retain Secure cookie and actual browser proof, not a test bypass. No secret traces/screenshots/logging.

Dependencies are already installed; do not reinstall/research them absent a concrete version failure. Existing npm vulnerability counts are not permission for unrelated upgrades or a reason to skip coding; record their actual relevance if needed within approved named-package metadata scope. No extra worker, model change, credential access, scheduled task, push or unrelated work.

Write progress after repository exists and each meaningful runtime milestone. Remaining worker continuation bounded to60 tool turns/3200seconds inside original90minute execution allowance. Do focused core work first. Research after an actual repeated evidence-layer failure, not because writing the service is substantial. If another concrete blocker remains after the permitted evidence-based repair/fallback, preserve the actual logs and code and say exactly which handler/test cannot work. Final handoff requires nonempty integration/browser evidence and cleanup; Codex reviews independently.
