Implement the complete approved W3-02a isolated attendance backend now as hermes-forge-w3-02a. Current contract: docs/agent-packets/PAY-W3-02-LOCAL.json. This exact user-approved job supersedes historical deferred-Hermes/intake-only instructions only for this wave. Same Forge/Terra900k/medium provider; no children or model fallback. Parent concurrently owns launcher/harness/package/UI files; do not edit them or treat their expected changes as stale input.

Read approved design/implementation and current accepted modules, then write focused RED tests and implement the whole backend in owned files. Build normal small typed readable modules; no throwaway mocks for claimed runtime. Use only installed pg/argon2/Node24/http/crypto. TypeScript server/main must run with native Node24 --experimental-transform-types and explicit .ts relative imports (parent supplies tsconfig allowImportingTsExtensions); avoid extensionless imports into existing web modules. Public runtime must never import web auth/admin/payroll/database repositories. Private kiosk-admin.ts may reuse AuthRepository/DPAPI. Use type-only shared contracts/ports.

Interfaces to coordinate: export createAttendanceServer(options) returning node:http.Server, with explicit injected repository/service dependencies if needed for tests; main.ts must listen on127.0.0.1:46218 using PAYSLIP_INGRESS_DATABASE_URL, close on SIGINT/SIGTERM, validate no arbitrary bind. GET /health proves DB query readiness and returns no data; POST /clock, POST /reconcile; deny all other backend routes. Parent later adds only exact static kiosk assets through this same server once W3-02a is verified. Export a pg-backed repository factory/class and document how parent seeds/enrolls synthetic devices and employees. Authentication token may be HttpOnly cookie or Authorization Bearer; parent kiosk fixture can set an enrolled device cookie. Do not expose a browser page containing token values. Add owner-only enroll/rotate/revoke and employee PIN-setting commands; never persist/display raw secrets in progress/tests. Existing fixture helpers return actor id as .id (not.userId).

The parent will extend scripts/windows/test-database.ps1 to support TaskId PAY-W3-02a, database payslip_w3_02_synthetic; tests must create their own exact URL guard (do not import W2-specific testPool). Parent supplies PAYSLIP_TEST_DATABASE_URL for private app fixture setup and PAYSLIP_TEST_INGRESS_DATABASE_URL for separate limited-role requests. Design one additive migration, then tell parent the exact tables/columns/privileges the dedicated ingress role needs. No SQL privilege widening for immutable histories: SELECT FOR UPDATE requires UPDATE privilege, so use mutable employee/device aggregates to serialize and read immutable sources normally.

Crucial clock semantics: server assigns raw UTC timestamp once and durable transaction precedes recorded:true. A repeat idempotency key reuses original timestamp and event; don't hash the new retry time as a new payload. Pin a changed action conflict. Serialize one employee's IN/OUT and verify invalid/concurrent states. Wrong code/PIN are generic with equivalent password verification effort. Rate-limit hashes/keys, never raw PIN/codes in log payloads. Dedicated ingress DB role cannot read payroll, user passwords, email, full employee names or documents. A small separate redacted kiosk audit table is appropriate because existing audit_events requires an admin user actor.

Reconciliation after lost response cannot retain a PIN: use the original random UUID key as a short-lived per-command proof bound to the successfully authenticated employee and exact device/session. It must only reveal that one outcome; no new clock authority, cross-device/employee access or secret logging. A known failure before commit returns recorded:false; uncertain COMMIT acknowledgement or lost response must stay pending_confirmation (not definitive rejection). Tests must inject these distinct failures without a public test-only endpoint.

Write docs/agent-packets/PAY-W3-02a-HERMES-progress.json within the first few minutes, then at each milestone: session ID, changes, RED/GREEN, parent DB readiness, schema/least-privilege grants, exact blockers. Write docs/planning/W3-02a-ingress-contract.md and structured docs/agent-packets/PAY-W3-02a-HERMES-handoff.json on completion. Return for parent verification once code/tests are ready; do not spend the budget on UI, wrappers or broad infrastructure. If terminal blocks a script, report to parent and continue scoped work; never bypass policy. Existing installed pure tests/typechecks allowed; parent owns DB/HTTP/browser orchestration. No install/network research, real data, .env, outside-project edits, recursion, staging/commit/push. Limit120 turns/3600seconds, core checkpoint15min.
