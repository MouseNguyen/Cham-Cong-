Implement PAY-W3-03 attendance review/backend now as hermes-forge-w3-03. Read docs/agent-packets/PAY-W3-03.json plus current design, implementation W3-03 and approved attendance/auth/scheduling modules. User approved this exact next step and delegation as needed; same Forge/Terra900k/medium route overrides historical deferred-Hermes wording for this task only. No children, provider/model change, external data, installs or global files.

Own the complete bounded backend: contracts, deterministic review/snapshot helper, authenticated repository, thin commands/query, one additive SQL migration if needed, focused real PostgreSQL integration tests and synthetic fixture. Do NOT modify prisma/schema.prisma: supply complete new model fragment in docs/agent-packets/PAY-W3-03-schema-fragment.prisma for parent lifecycle review; do not repeat the prior rejected optional unique metadata edit. The existing attendance_adjustments require approved_by and are not a draft store. Prefer clearly typed append-only proposal/decision tables with complete audit data, reuse existing immutable attendance_snapshots for frozen output. Never label a maker as approver to squeeze drafts into an approved table.

API agreement to enable parent UI: export AttendanceReviewRepository(pool, protector, {organizationId,now?}); public methods list(sessionToken,{employeeId,workplaceId,month:'YYYY-MM'}), draft(credentials,{employeeId,workplaceId,month,expectedVersion,sourceHash,segments,exclusions,reason}), approve(credentials,{employeeId,workplaceId,month,expectedVersion,candidateHash,reason}), finalize(credentials,{employeeId,workplaceId,month,expectedVersion,candidateHash}). credentials={sessionToken,csrfToken}. Publish exact return and segment/exclusion types early in packages/contracts/src/attendance-review.ts and progress. list returns role/can flags, current version/sourceHash/candidateHash, raw events, scheduled intervals, suggested segments, blockers, proposal/decision state, optional frozen snapshot. Parent owns HTTP and UI wrappers.

Use real current stored sources, not caller-supplied approved flags. Period is shop month in Asia/Ho_Chi_Minh. Load all raw events within period plus needed boundary IN/OUT context; do not silently omit missing/duplicate/out-of-order/straddling evidence. Exact millisecond intervals, non-overlapping and clipped/bound to period; every required source event must have an explicit proposal disposition (approved segment sourceEventIds or excluded event with reason). Corrections can supply missing time only as explicit reasoned maker proposal approved by a separate owner. Preserve source event IDs, raw timestamps and schedule references; rejected/excluded rows stay in snapshot and never enter payable total. Holiday/night/out-of-schedule classifications must retain their evidence and not be silently changed to ordinary. Reuse detectAttendanceExceptions, schedule resolver, exact segment validation where useful; don't invent legal rates/deductions. Unknown schedule or unresolved unsupported scope remains a structured blocker.

Maker is accountant, checker/finalizer is fresh-MFA owner, distinct authenticated identity. Validate live CSRF/org/session/role/MFA again inside write transaction. Capture source seal for raw, schedule, holiday, correction/version evidence; current evidence or new proposal invalidates previous approval and stale finalization. Guard races by consistent workplace/employee locks shared with scheduling/clock insertion and optimistic version. Store actor/time/reason/source segment/expected version in immutable proposal/decision/audit records. Final snapshot canonical payload includes approved segments plus exclusions and source evidence, canonicalization/schema version, approval identity/time. Concurrent/repeated finalize must return same stored snapshot ID/hash (no duplicate audit or mutable existing snapshot). Recompute hashes from stored payload; never trust content_hash alone.

Test real maker/checker separation, no CSRF/foreign org/revoked/stale MFA, unresolved and stale source/correction candidate, raw exclusion coverage, approved vs rejected totals, exact 59/60/61s, overlap, immutable stored snapshot, concurrent/replayed finalization, atomic rollback. Do not claim completeness from static checks or fake repositories. Parent owns DB startup/migrate runtime and runs tests against PAYSLIP_TEST_DATABASE_URL guarded to payslip_app@127.0.0.1:55432/payslip_w3_03_synthetic. Reuse auth/support scenario(pool),login,protector but never its W2-specific testPool guard. You may run focused pure RED/GREEN and typecheck with existing tools; no PG server/migration or app server yourself. No package manager installs. Keep code readable, not minified. Budget 100 turns/2400s; publish progress early, finish a runnable core before polish, no task decomposition.
