Continue the same PAY-W3-03 session for ONE bounded domain repair while parent independently owns repository, migration, schema lifecycle, HTTP/UI and integration tests. New exclusive write scope for this repair: packages/attendance-domain/src/review.ts and packages/attendance-domain/test/review.spec.ts ONLY. Contracts, repository, migrations and integration fixtures are now parent-owned; do not edit them. No children, provider change, installs, database/server/browser work. Existing approval remains same model and task; this narrows your role.

Parent source review found the current helper cannot resolve anomalies: validateReviewCandidate always appends raw anomalies even after explicit correction or exclusion. It also accepts changed raw boundaries without correction metadata, ignores classification validity/holiday downgrade, treats only in-period events as coverage while loading unrelated neighboring events, and does not canonically normalize segment/exclusion ordering. Fix these core blockers with focused failing tests first.

Preserve exports reviewPeriod, canonicalReview, hashReview, suggestedReviewSegments, validateReviewCandidate, types ReviewSource/ReviewCandidate. Add export normalizeReviewCandidate(candidate): ReviewCandidate with deep runtime validation, stable segment ordering by start/end/id, exclusions by eventId, sorted deduplicated sourceEventIds/scheduleRefs, trimmed bounded reasons, unique nonempty IDs, supported classification only, safe finite UTC ms. Do NOT silently discard invalid duplicate source references. Parent will normalize before storing candidate. canonicalReview must deterministically reject unsupported/undefined/NaN values, not hash malformed text.

ReviewSource will carry rawEvents, scheduledIntervals, holidayLocalDates; parent may additionally include scheduleEvidence and missingScheduleLocalDates in source for hash binding. Treat source rawEvents as the EXACT required source set, including only relevant month-boundary IN/OUT context (parent filters irrelevant neighbors). An interval crossing the month must preserve both source IDs even if one is outsidePeriod; allow a correctly clipped boundary without calling it a correction. Never allow an undisposed required source ID, duplicate disposition, overlap, or paid milliseconds outside month. Respect last-millisecond precision.

A normal segment must match the actual chronological IN/OUT pair clipped to month and have a supported classification matching source evidence. Changed start/end, missing/duplicate punches, manual intervals or re-pairing require explicit correction {source:'maker_proposal',reason:nonempty}; these source anomalies resolve only when every affected event is explicitly covered by a reasoned correction or exclusion. Exclusions remain explicit and never enter payable total. Fully excluded missing/duplicate/raw anomaly evidence can resolve without inventing paid time. Unknown/missing schedule for paid segments remains a blocker. Out-of-schedule, daytime holiday and night-work can be explicitly classified and approved; ordinary cannot silently cover them. Simultaneous holiday/night may stay an explicit unsupported blocker if the current classification contract cannot represent it; state that limit, do not invent legal rates. Raw anomaly list before a proposal is useful, but a valid resolved candidate should not retain resolved anomaly blockers.

Suggestions must not silently label night/holiday/out-of-schedule time ordinary. Split sensible known boundaries or return an explicit unresolved blocker. Exact interval/source/schedule references remain inspectable. Keep suggestedReviewSegments deterministic; correction defaults must never fabricate a paid missing OUT. Validate 59/60/61s, valid corrected missing punch, complete exclusion, ordinary holiday downgrade rejection, changed times without correction rejection, cross-month source coverage, duplicate dispositions, stable reversed input ordering, and malformed/runtime tampering inputs.

Write concise result and exact pure-test command/count into docs/agent-packets/PAY-W3-03-HERMES-progress.json ONLY as status (this one status file remains allowed); no end-to-end or database acceptance claim. Max 60 turns/1500 seconds, two same-layer attempts. Parent is actively integrating the repository and will run actual PG/UI tests. Finish this pure helper repair; don't expand architecture.
