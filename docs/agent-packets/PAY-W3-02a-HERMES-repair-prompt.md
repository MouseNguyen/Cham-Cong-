The first handoff is NOT accepted at the backend runtime layer. Continue this same approved W3-02a job and repair the concrete core issues below. Existing parent package/tsconfig and DB harness are now ready; do not claim they are missing. Do not edit parent files. No intake/permission request needed, no new delegation/model. One consolidated repair pass, maximum60 turns/1800seconds, within original wave.

Direct evidence: parent npm run build fails at apps/web/src/lib/db/repositories/kiosk-admin.ts import '../auth.ts' (actual AuthRepository sibling is './auth'). Public PG repository replay SQL selects occurred_at from kiosk_command_proofs although that table has no occurred_at. Only MemoryIngressRepository has been tested; its fabricated outcomes do not prove PostgreSQL transaction behavior. It also does not model authorization or concurrency faithfully. Replace/expand ingress.spec.ts with actual PG-backed fixtures (keep separate memory tests only if useful) and ingress-support.ts. Exact guards: PAYSLIP_TEST_DATABASE_URL=>payslip_app@127.0.0.1:55432/payslip_w3_02_synthetic; PAYSLIP_TEST_INGRESS_DATABASE_URL=>payslip_ingress same database. Parent script ready: powershell -NoProfile -File scripts/windows/test-database.ps1 -Mode Green -TaskId PAY-W3-02a. Parent can execute it on your readiness report. Existing employee.fixture(pool) and input() can be reused; fixture actor ID is .id, credentials carry sessionToken/csrfToken. Synthetic PIN/token must be generated in memory and assertions must avoid dumping them.

Repair the complete downstream chain, not just the import:
1. Owner enrollment/rotation/revocation/PIN changes need CSRF credentials, current active session/role/fresh MFA checked transactionally, org/workplace/employee/device compatibility, meaningful expiry validation, and transactional audit. Use approved owner guard and existing auth patterns. PIN provisioning plus employee-state initialization must be atomic. Do not allow cross-org/device PIN rows. Add composite FKs where necessary.
2. Employee clock state must serialize the employee across ALL devices, not per device. One employee concurrently IN from two enrolled devices must yield exactly one event. Replay lookup must occur after serialization, join clock_events for original time, and return the original outcome for a valid authenticated repeat even after ten-minute reconcile-proof expiry. Changed action with same key is a conflict.
3. Recheck live device session/revocation/expiry and employee active/shop binding inside the recording transaction. Do not grant UPDATE on employee history just for locks. Device advisory transaction locks shared by private rotate/revoke and public record/reconcile are one narrow way to close the race. For concurrent employee deactivation, a fixed-search-path SECURITY DEFINER BEFORE INSERT trigger that locks only the referenced active employee row can preserve ingress column-only SELECT privileges; do not add broad privilege grants.
4. kiosk_rate_limits is currently dead storage: enforce durable bounded per-device/per-code attempts and generic unknown-code/wrong-PIN replies. Keep equivalent Argon2 verification for known/unknown codes; cache a dummy hash rather than generating an expensive hash on each attempt. Use request IDs, redacted audit, and no raw PIN/token/code/key logging.
5. Real PG recordClock must distinguish known pre-COMMIT failure from uncertain COMMIT acknowledgement. The current catch simply throws, so the claimed {recorded:false}/pending behavior only exists in a fake. Track commit attempt; rollback known earlier failures, return pending for uncertain commit, preserve actual persisted proof for reconciliation. Include a test-only injected driver wrapper that fails before commit and after real COMMIT, never a public test endpoint. Keep callbacks internal and ensure no artificial runtime route can toggle them.
6. The approved wire contract uses action CLOCK_IN/CLOCK_OUT and success recordedAt/nextAllowedAction. Map to stored IN/OUT internally; update tests/contracts/ports. Reconciliation must accept employeeCode plus original idempotencyKey (not employeeId, which the browser does not know when the first response is lost). Resolve the employee internally from device-bound code and require the original successful authentication proof for that exact device/session/employee/key; no retained PIN or new clock authority. Expire only reconciliation proof access, not historical event idempotence.
7. Keep exact HTTP allowlist; deny non-local Host/Origin for this loopback wave. Invalid credentials/state/known rejection must return recorded:false plus generic Vietnamese message/code/nextAction. Unknown commit/transport remains pending_confirmation. Request ID should be generated server-side. Health proves only DB readiness.
8. Add real DB/HTTP tests covering durable IN/OUT/replay after server clock changes, original timestamp/one audit, same-key/action conflict, concurrent same employee across sessions/devices, invalid OUT, wrong-code/PIN same response, rate limit, revocation/rotation, cross-shop/PIN setup denial, CSRF/accountant/stale-MFA private denials, limited-role forbidden employee-name/payroll/users/PDF SELECT, rollback and post-COMMIT uncertainty/reconciliation, and actual denied ingress paths. No production claim or pass from stub-only tests.

Existing model wrote six new tables. Keep the schema count/projections consistent; parent updates the shared schema-denominator test. Migration currently grants payslip_ingress column-level employee SELECT (good); do not widen it. Parent creates that role before all new migrations. Native Node24 supports --experimental-transform-types; explicit .ts imports allowed by current root tsconfig. Validate own source with existing tsc. Then update progress and handoff honestly with exact DB runtime not_run until parent runs it. Do not touch UI, launcher, root configs, old migrations or completed domain code.
